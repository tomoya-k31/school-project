#include <stdio.h>
#include <string.h>
#include <stdlib.h> 
#include <limits.h>

int main(int argc, char *argv[])
{
  int x;
  scanf("%d", &x);
  double *data = malloc(sizeof(double)*x);
  for (int i=0;data;i++)
    scanf("%lf",data[i]);
}
